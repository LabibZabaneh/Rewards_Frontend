import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        borderRadius: '30%',
        marginBottom: '3%',
    },
    digit: {
        fontSize: 55,
        color: '#4A2C2A',
        fontWeight: 'bold'
    }
})