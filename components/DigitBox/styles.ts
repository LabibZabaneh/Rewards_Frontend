import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        maxWidth: '12.5%',
        maxHeight: '20%',
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: '#CCC',
        borderRadius: '30%',
        marginBottom: '3%'
    },
    digit: {
        fontSize: 50,
    }
})